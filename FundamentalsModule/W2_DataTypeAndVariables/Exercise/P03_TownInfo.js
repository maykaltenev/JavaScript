function townInfo(townName, population, area) {
    let town = townInfo;
    let people = Number(population);
    let square = Number(area);
    
    console.log(`Town ${townName} has population of ${population} and area ${area} square km.`);
}
townInfo('Sofia', 1314123, 54);